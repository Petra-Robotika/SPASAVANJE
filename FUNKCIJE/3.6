#pragma once
#include "helper.h"

void RobotLine::radionica()
{
  armOpen();
wallFollowRight();
   if (front() < 120) {
    go(50, 50);
    delayMs(200);
    go(-90, 90);
    delayMs(500);
    armCatchReady();
    armCatch();
    

  }
    

  }
